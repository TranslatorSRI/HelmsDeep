# Translator Performance Test Runner

This load tester sends a corpus of load queries to a specified Translator service.

To get started, activate your virtual environment and run:

```bash
pip install -e .
```

```bash
run_performance_tests --targets kps
```
